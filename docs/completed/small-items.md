# Small Fixes

## Transactions

- When selected multiple transactions, panel is shown above the table. Instead that just show a button next to new transaction with dropdown for actions. Button text could be 3 selected.